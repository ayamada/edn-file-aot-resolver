(ns edn-file-aot-resolver.core-test
  (:require [clojure.test :refer :all]
            [edn-file-aot-resolver.core :as core]))

(deftest efar-test
  (let [
        ]
    (testing "TODO: write descriptions"
      (is (= [] []))
      true)))
